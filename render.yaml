services:
  - type: web
    name: somnium
    runtime: static
    buildCommand: echo "Static site ready"
    staticPublishPath: ./web
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
